# MCP-Jira
# LLM Task Manager  **Jira léger pour LLMs** (Projet/Epic/Story/Sprint/Doc).   **REST API + MCP Tools** (Claude crée stories direct !)  ## Stack FastAPI + Cloud SQL PG + Cloud Run + MCP SDK  ## Quickstart ```bash uv sync uv run uvicorn app.main:app --reload
